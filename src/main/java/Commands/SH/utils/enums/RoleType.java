package Commands.SH.utils.enums;

/**
 * the different Roles including unique player types
 * TODO: consider moving towards using Strings for roletypes instead as the base
 */
public enum RoleType {
    Fascist,
    Liberal,
    Hitler
}
